// Minimal LFR for a REACTION CHAMBER (matches reaction_chamber.mint).
module reaction_chamber(
    finput in_1,
    foutput out_1
);
    #MAP "REACTION CHAMBER" "~"
    assign out_1 = ~in_1;
endmodule
