# Copy to .env.production and fill in before building (only needed when backend is not same-origin).

# Full URL of Neptune_2026 backend API (e.g. your server or ngrok)
# VUE_APP_API_BASEURL=https://your-backend.example.com

# Socket.IO URL (default is current hostname:3000)
# VUE_APP_SOCKET_URL=https://your-backend.example.com
